/**
 * Application layer: use-case services and orchestration.
 *
 * <p>May depend on {@code com.autograder.domain} and on Spring stereotypes
 * ({@code @Service}, {@code @Component}). Must not depend on
 * {@code infrastructure} or {@code web}.
 */
package com.autograder.application;
