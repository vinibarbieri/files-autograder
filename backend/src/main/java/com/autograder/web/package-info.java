/**
 * Web layer: REST controllers, DTOs, exception translators.
 *
 * <p>Depends on {@code application} and {@code domain}. HTTP/JSON concerns
 * live here and must not leak into the application layer.
 */
package com.autograder.web;
